import React from "react";
import Navbar from 'react-bootstrap/Navbar'

const HeaderContainer = () => {
    return (
        <Navbar expand="lg" variant="dark" bg="dark">
            <Navbar.Brand>My Marketplace</Navbar.Brand>
        </Navbar>
    );
};

export default HeaderContainer;
